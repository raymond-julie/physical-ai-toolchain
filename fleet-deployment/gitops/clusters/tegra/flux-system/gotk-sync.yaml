# This manifest was generated by flux. DO NOT EDIT.
# ---------------------------------------------------------------------------
# Ported from the source GitOps repo for the physical-ai-toolchain monorepo:
#   * spec.url is a PLACEHOLDER (<your-org>). Set it to your fork before
#     bootstrap, or let `flux bootstrap git --url=...` regenerate this file.
#   * spec.path was rewritten to the monorepo location
#     (./fleet-deployment/gitops/clusters/tegra).
# See clusters/tegra/README.md for the bootstrap steps.
# ---------------------------------------------------------------------------
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/<your-org>/physical-ai-toolchain
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./fleet-deployment/gitops/clusters/tegra
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
