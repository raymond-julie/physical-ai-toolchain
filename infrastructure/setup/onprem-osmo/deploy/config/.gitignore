# Operator-specific cluster inventory (contains real IPs / hostnames / SSH users).
inventory.env
inventory.local.env

# Runtime artifacts generated by deploy.ps1 during a deployment.
kubeconfig
join-command.sh
pending-labels.txt
