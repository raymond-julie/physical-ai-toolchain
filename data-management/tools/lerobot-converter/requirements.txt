# Runtime dependencies for the MCAP -> LeRobot v2.1 converter and inspection CLIs.
# Install into a standalone virtualenv: python -m pip install -r requirements.txt
av>=12.0                  # PyAV: H.264 (libx264) video encoding
numpy>=1.26
pyarrow>=15.0             # per-episode parquet writing
mcap>=1.1                 # MCAP reader
mcap-ros2-support>=0.5    # provides the mcap_ros2 CDR decoder
Pillow>=10.0              # JPEG decode (imported as PIL)
tqdm>=4.66
